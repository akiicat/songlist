<%# params: songlsits %>
<%= form_for(songlist, url: dashboard_songlist_path(songlist), method: :patch, remote: true) do |f| %>
  <%= render 'dashboard/songlists/form/form', songlist: songlist, f: f %>
<% end %>
