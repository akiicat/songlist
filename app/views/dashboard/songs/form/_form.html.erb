<% if song.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

    <ul>
    <% song.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="field">
  <%= f.label :singer_id %>
  <%= f.collection_select :singer_id, Singer.all, :id, :name, prompt: true %>
</div>

<div class="field">
  <%= f.label :composer_id %>
  <%= f.collection_select :composer_id, Singer.all, :id, :name, prompt: true %>
</div>

<div class="field">
  <%= f.label :title %>
  <%= f.text_field :title %>
</div>

<div class="field">
  <%= f.label :title_translation %>
  <%= f.text_field :title_translation %>
</div>

<div class="field">
  <%= f.label :description %>
  <%= f.text_field :description %>
</div>

<div class="field">
  <%= f.label :lyric_url %>
  <%= f.text_field :lyric_url %>
</div>

<div class="field">
  <%= f.label :video_url %>
  <%= f.text_field :video_url %>
</div>

<div class="actions">
  <%= f.submit %>
</div>
